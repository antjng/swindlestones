import * as THREE from 'three';

export const PLAYER_DICE_CENTER = new THREE.Vector3(-1.4, 0, 0.2);
export const AI_DICE_CENTER = new THREE.Vector3(2.3, 0, -1.8);

/** The player's wrist; the fingers reach toward the opponent. */
export const PLAYER_HAND_REST = new THREE.Vector3(-5.0, 0.45, 4.6);

export const OPPONENT_X = 4.7;
export const OPPONENT_Z = -3.8;
